<UserControl x:Class="UnoGame.WPFApp.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UnoGame.WPFApp.Views"
             xmlns:vm="clr-namespace:UnoGame.WPFApp.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             d:DataContext="{d:DesignInstance Type=vm:MainMenuViewModel}"
             mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome to" FontSize="50" Foreground="White" FontFamily="Bahnschrift" HorizontalAlignment="Center" />
            <TextBlock Text="UNO GAME" FontSize="80" Foreground="White" FontFamily="Bahnschrift" HorizontalAlignment="Center" />
            <Button Content="Start Game" Command="{Binding StartGameCommand}" FontFamily="Bahnschrift" FontSize="30" Width="200" Height="60" Margin="0,100,0,0" />
            <Button Content="Exit Game" Command="{Binding ExitGameCommand}" FontFamily="Bahnschrift" FontSize="30" Width="200" Height="60" Margin="0,30,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
