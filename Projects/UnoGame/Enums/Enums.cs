namespace Enums;

public enum CardColor { Red, Blue, Green, Yellow }

public enum CardNumber { Zero, One, Two, Three, Four, Five, Six, Seven, Eight, Nine }

public enum ActionType { Skip, Reverse, DrawTwo, Wild, WildDrawFour }